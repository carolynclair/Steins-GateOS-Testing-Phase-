{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "install-live-build",
      "type": "shell",
      "command": "sudo apt-get update && sudo apt-get install -y live-build",
      "problemMatcher": []
    }
  ]
}